{
  "framework": null,
  "installCommand": "npm install",
  "buildCommand": "DISABLE_ESLINT_PLUGIN=true CI=false npx react-scripts build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
