# API Keys and Configuration
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# Demo mode: when true, guests see placeholder basil data without login
# Defaults to false. Set to true for demos when sensors are not connected.
DEMO_ENVIRONMENT=false

# OpenAI API Key for chatbot functionality
OPENAI_API_KEY=your_openai_api_key_here

# NWS (National Weather Service) API User-Agent
# Format: AppName-your.email@domain.com
NWS_USER_AGENT=SmartPlantAssistant-tyler.i.hughes@vanderbilt.edu

# Flask Secret Key (auto-generated if not set, but recommended to set a fixed value)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=

# Database URL (defaults to SQLite if not set)
# DATABASE_URL=sqlite:///smart_plant.db

# Flask Secret Key (CRITICAL for sessions - must be consistent!)
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
# If not set, a fixed development key will be used (sessions will persist)
SECRET_KEY=
